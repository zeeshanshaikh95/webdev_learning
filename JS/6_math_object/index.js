// Math = built-in js object that provides a
//        collection of properties and methods

let x = 3;
let y = 2;
let z = 1;

console.log(Math.PI);
//z = Math.round(x);
//z = Math.floor(x);    //rounds of to lower value
//z = Math.ceil(x);      //rounds of to higher value
//z = Math.trunc(x);    //eliminates the decimal
//z = Math.pow(x, y);
//z = Math.sqrt(x);
//z = Math.log(x);
//z = Math.sin(x); //radians value
//z = Math.cos(x);
//z = Math.tan(x);
//z = Math.abs(x);  //positive number
//z = Math.sign(x);     // if negative -1, positive +1, if 0 then 0
// let max = Math.max(x, y, z);  // finds max comparatively
let min = Math.min(x, y, z);

console.log(min);
